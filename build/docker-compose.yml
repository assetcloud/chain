version: '3'

services:
  chain:
    build:
      context: .

  chain32:
    build:
      context: .
    command: ["/root/chain", "-f", "/root/chain-solo.toml"]

  chain31:
    build:
      context: .
    command: ["/root/chain", "-f", "/root/chain-solo.toml"]

  chain30:
    build:
      context: .
    command: ["/root/chain", "-f", "/root/chain-solo.toml"]

  chain29:
    build:
      context: .
    command: ["/root/chain", "-f", "/root/chain-solo.toml"]

  chain28:
    build:
      context: .
    command: ["/root/chain", "-f", "/root/chain-solo.toml"]
